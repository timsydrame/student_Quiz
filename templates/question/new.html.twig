{% extends "base.html.twig" %}
{% block title %}Create new Question!
{% endblock %}

{% block body %}
<div class="form-container">
    {# templates/question/new.html.twig #}
    {{ form_start(form) }}
    {{ form_errors(form) }}
    
    {{ form_row(form.quiz) }}
    {{ form_row(form.enonce) }}
    <button type="submit">Enregistrer la question</button>

    {{ form_end(form) }}
</div>
{% endblock %}
